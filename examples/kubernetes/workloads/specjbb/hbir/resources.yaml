apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: specjbb
spec:
  template:
    spec:
      containers:
        - name: backend
          resources:
            requests:
              cpu: 5
              memory: 8G
