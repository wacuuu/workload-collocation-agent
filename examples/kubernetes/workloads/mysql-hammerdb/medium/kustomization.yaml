nameSuffix: -medium
bases:
- ../base

commonLabels:
  app: mysql-medium
  size: medium

configMapGenerator:
  - name: hammerdb
    behavior: merge
    literals:
      - number_runners=100
      - count_ware=100
      - worker_building_schema=100

patchesStrategicMerge:
  - resources.yaml
