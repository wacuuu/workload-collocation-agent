nameSuffix: -big
bases:
- ../base

commonLabels:
  app: redis-big

configMapGenerator:
- name: memtier
  behavior: merge
  literals:
    - clients=50
    - threads=8
    - datasize=1024
    - ratio=1:10
