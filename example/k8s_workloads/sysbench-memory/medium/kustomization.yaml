nameSuffix: -medium

bases:
- ../base

commonLabels:
  app: sysbench-memory-medium

patchesStrategicMerge:
  - resources.yaml

configMapGenerator:
- name: sysbench-memory
  behavior: merge
  literals:
    - threads=8
    - memory=64g
    - report_interval=45