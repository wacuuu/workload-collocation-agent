nameSuffix: -big

bases:
- ../base

commonLabels:
  app: sysbench-memory-big

patchesStrategicMerge:
  - resources.yaml

configMapGenerator:
- name: sysbench-memory
  behavior: merge
  literals:
    - threads=16
    - memory=128g
    - report_interval=90
