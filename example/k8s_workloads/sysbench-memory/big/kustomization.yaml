nameSuffix: -big

bases:
- ../base

commonLabels:
  app: sysbench-memory-big

patchesStrategicMerge:
  - resources.yaml

configMapGenerator:
- name: sysbench-memory
  behavior: merge
  literals:
    - threads=8
    - memory=4g
    - report_interval=1
