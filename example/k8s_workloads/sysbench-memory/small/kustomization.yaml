nameSuffix: -small
bases:
- ../base

commonLabels:
  app: sysbench-memory-small

patchesStrategicMerge:
  - resources.yaml

configMapGenerator:
- name: sysbench-memory
  behavior: merge
  literals:
    - threads=4
    - memory=16g
