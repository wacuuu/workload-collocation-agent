nameSuffix: -medium
#commonLabels:
#        app: redis
bases:
- ../base
#patchesStrategicMerge:
#- overlay.yaml

configMapGenerator:
- name: redis-config
  literals:
    - memtire_cpus=0-1000
    - redis_cpus=0-1000
    - clients=100
    - threads=4
    - datasize=512
    - pipeline=1
    - keymaximum=40000
    - ratio=1:10
    - redis_extra=
    - memtire_extra=""
